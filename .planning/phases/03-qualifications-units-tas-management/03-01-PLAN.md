---
phase: 03-qualifications-units-tas-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/.env.example
  - apps/api/.env
  - apps/api/package.json
  - apps/web/package.json
autonomous: true
requirements: [TAS-04, TAS-05]
tags: [schema, prisma, s3, env, scaffold]

must_haves:
  truths:
    - "TasDocument table has columns for qualification_id, version_label, status, file_key, file_name, file_size, review_date, uploaded_by_id"
    - "TasDocumentUnit junction table exists linking tas_document_id to unit_id with unique pair index"
    - "Qualification model has tas_documents back-relation; User model has tas_uploads back-relation"
    - "apps/api/.env.example documents required S3_REGION, S3_ENDPOINT, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_BUCKET keys"
    - "@aws-sdk/client-s3 and @aws-sdk/s3-request-presigner installed in apps/api; @radix-ui/react-select and @radix-ui/react-collapsible installed in apps/web"
    - "prisma db push applied; npx prisma validate exits 0"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Expanded TasDocument model and TasDocumentUnit junction"
      contains: "model TasDocumentUnit"
    - path: "apps/api/.env.example"
      provides: "S3 env var documentation"
      contains: "S3_BUCKET"
    - path: "apps/api/package.json"
      provides: "AWS SDK dependencies"
      contains: "@aws-sdk/client-s3"
    - path: "apps/web/package.json"
      provides: "Radix Select + Collapsible deps"
      contains: "@radix-ui/react-select"
  key_links:
    - from: "TasDocument.qualification_id"
      to: "Qualification.id"
      via: "Prisma @relation"
      pattern: "qualification\\s+Qualification\\s+@relation"
    - from: "TasDocumentUnit.unit_id"
      to: "Unit.id"
      via: "Prisma @relation"
      pattern: "unit\\s+Unit\\s+@relation"
---

<objective>
Wave 0/1 schema and environment foundation for Phase 3. Expand the stub `TasDocument` model with all fields required by D-13 (qualification_id, version_label, status, file_key, file_name, file_size, review_date, uploaded_by_id), add the `TasDocumentUnit` junction table for D-07 unit links, install the AWS SDK v3 modular packages (`@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`) and Radix UI primitives (`@radix-ui/react-select`, `@radix-ui/react-collapsible`), document S3 environment variables, and run `npx prisma db push` so all downstream waves can compile against the live database.

Purpose: All TAS endpoints (Plan 03) and TAS UI (Plan 05) depend on this schema. Qualification detail (Plan 02/04) also reads from the new TasDocument fields. The S3 SDK must be installed before TAS presign endpoints can be written.
Output: Schema migrated to Neon DB, env documented, new npm dependencies installed and lockfile updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qualifications-units-tas-management/03-CONTEXT.md
@.planning/phases/03-qualifications-units-tas-management/03-RESEARCH.md
@.planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md
@packages/database/prisma/schema.prisma
@CLAUDE.md

<interfaces>
Current `TasDocument` stub (schema.prisma lines 84–95):
  id, rto_id, rto (relation), created_at, updated_at, deleted_at

Existing junction pattern to copy (schema.prisma lines 216–228, QualificationUnit):
  id (UUID), qualification_id, qualification @relation, unit_id, unit @relation,
  created_at, @@unique([qualification_id, unit_id]), @@index entries, @@map.

Existing back-references on Rto (schema.prisma line 41): `tas_documents TasDocument[]`.
New back-references required:
- On Qualification: `tas_documents TasDocument[]`
- On User: `tas_uploads TasDocument[]`
- On Unit: `tas_document_units TasDocumentUnit[]`
- On TasDocument: `units TasDocumentUnit[]`

Existing `rto.service.ts` placeholder uses `prisma.tasDocument.create({ data: { rto_id: rto.id } })` — all new TasDocument columns must be nullable or `@default(...)` so this call still compiles. (Per PATTERNS.md observation #3.)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database migration | Schema changes touch production data; backfill defaults required |
| Package install | npm packages introduce supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | prisma db push | mitigate | All new columns nullable or with @default so existing `rto.service.ts` placeholder TasDocument insert still compiles; no data loss expected (TasDocument empty in dev) |
| T-03-02 | Information Disclosure | .env S3 secrets | mitigate | Only document keys in `.env.example`; never commit real credentials to `.env` (already in .gitignore per Phase 1) |
| T-03-SC | Tampering | @aws-sdk/client-s3, @aws-sdk/s3-request-presigner, @radix-ui/react-select, @radix-ui/react-collapsible | mitigate | All four packages disposition Approved in RESEARCH.md Package Legitimacy Audit; AWS and Radix UI are verified publishers. Wave-1 install task verifies publisher and download count before install. |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-qualifications-units-tas-management/03-RESEARCH.md (sections "Standard Stack" and "Package Legitimacy Audit")
  </read_first>
  <what-built>Four npm packages staged for install. RESEARCH.md flagged them [ASSUMED] — slopcheck not run.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@aws-sdk/client-s3 — confirm publisher is `aws` org and weekly downloads > 10M.
    2. Open https://www.npmjs.com/package/@aws-sdk/s3-request-presigner — confirm same `aws` publisher.
    3. Open https://www.npmjs.com/package/@radix-ui/react-select — confirm publisher is `radix-ui` org, weekly downloads > 1M.
    4. Open https://www.npmjs.com/package/@radix-ui/react-collapsible — confirm publisher is `radix-ui`, weekly downloads > 1M.
  </how-to-verify>
  <resume-signal>Type "approved" or list any package that fails verification.</resume-signal>
  <acceptance_criteria>
    - Human confirms all four packages pass the publisher + download-count check.
    - No package is from an unknown publisher.
  </acceptance_criteria>
  <done>User has approved install for all four packages.</done>
</task>

<task type="auto">
  <name>Task 2: Install AWS SDK and Radix dependencies</name>
  <files>apps/api/package.json, apps/web/package.json</files>
  <read_first>
    - apps/api/package.json (current dependency list)
    - apps/web/package.json (current dependency list)
    - .planning/phases/03-qualifications-units-tas-management/03-RESEARCH.md (Standard Stack section)
  </read_first>
  <action>In `apps/api`, install `@aws-sdk/client-s3@^3.1048.0` and `@aws-sdk/s3-request-presigner@^3.1048.0` as runtime dependencies (`npm install` from inside `apps/api`). In `apps/web`, install `@radix-ui/react-select@^2.2.6` and `@radix-ui/react-collapsible@^1.1.12` as runtime dependencies. Do NOT use `--legacy-peer-deps` unless install otherwise fails; do NOT install AWS SDK in `apps/web`. After install, run `npm ls @aws-sdk/client-s3` in `apps/api` and `npm ls @radix-ui/react-select` in `apps/web` to confirm resolution.</action>
  <verify>
    <automated>cd apps/api && npm ls @aws-sdk/client-s3 @aws-sdk/s3-request-presigner && cd ../web && npm ls @radix-ui/react-select @radix-ui/react-collapsible</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/package.json` `dependencies` contains keys `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner`.
    - `apps/web/package.json` `dependencies` contains keys `@radix-ui/react-select` and `@radix-ui/react-collapsible`.
    - `npm ls` in both workspaces returns exit code 0 with no UNMET DEPENDENCY warning for the four packages.
    - `apps/web/package.json` does NOT add `@aws-sdk/*` entries.
  </acceptance_criteria>
  <done>All four packages installed in their correct workspace with lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 3: Expand TasDocument model and add TasDocumentUnit junction</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    - packages/database/prisma/schema.prisma (entire file — must see User, Rto, Qualification, Unit, QualificationUnit, current TasDocument stub)
    - .planning/phases/03-qualifications-units-tas-management/03-RESEARCH.md (section "Expanded TasDocument Schema")
    - .planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md (junction table pattern)
    - apps/api/src/rto/rto.service.ts (to confirm placeholder `prisma.tasDocument.create` still compiles after expansion)
  </read_first>
  <action>
    In `packages/database/prisma/schema.prisma`:

    1. Replace the existing `TasDocument` stub block (lines 84–95) with an expanded model that keeps the existing `id`, `rto_id`, `rto`, `created_at`, `updated_at`, `deleted_at`, `@@index([rto_id])`, `@@index([deleted_at])`, `@@map("tas_documents")` AND adds:
       - `qualification_id String? @db.Uuid` (nullable so existing placeholder `create({ data: { rto_id } })` still compiles; backfill non-null in Plan 03 service layer)
       - `qualification Qualification? @relation(fields: [qualification_id], references: [id])`
       - `uploaded_by_id String? @db.Uuid`
       - `uploaded_by User? @relation(fields: [uploaded_by_id], references: [id])`
       - `version_label String @default("")` (default empty so legacy rows do not break)
       - `status String @default("Draft")` (values: Draft | Current | Archived; enforced in DTO layer)
       - `file_key String @default("")`
       - `file_name String @default("")`
       - `file_size Int @default(0)`
       - `review_date DateTime? @db.Date`
       - `units TasDocumentUnit[]` back-relation
       - Add indexes `@@index([qualification_id])` and `@@index([status])` in addition to the existing two.

    2. Append a new `TasDocumentUnit` model immediately after `TasDocument` following the `QualificationUnit` pattern (schema.prisma lines 216–228) exactly: UUID PK, `tas_document_id`, `tas_document` relation, `unit_id`, `unit` relation, `created_at`, `@@unique([tas_document_id, unit_id])`, `@@index([tas_document_id])`, `@@index([unit_id])`, `@@map("tas_document_units")`.

    3. Add `tas_documents TasDocument[]` back-relation to the `Qualification` model (after the existing `snapshots QualificationSnapshot[]` line).

    4. Add `tas_uploads TasDocument[]` back-relation to the `User` model (after the `refresh_token` field block, before timestamps — or anywhere within the relations area).

    5. Add `tas_document_units TasDocumentUnit[]` back-relation to the `Unit` model (after `snapshots UnitSnapshot[]`).

    6. Run `npx prisma format` then `npx prisma validate` from `packages/database/`.
  </action>
  <verify>
    <automated>cd packages/database && npx prisma validate && npx prisma format --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "model TasDocumentUnit" packages/database/prisma/schema.prisma` returns exactly one match.
    - `grep -n "qualification_id\\s\\+String?\\s\\+@db.Uuid" packages/database/prisma/schema.prisma` returns at least one match within the TasDocument block.
    - `grep -n "tas_documents TasDocument\\[\\]" packages/database/prisma/schema.prisma` returns at least two matches (one on Rto, one on Qualification).
    - `grep -n "@@unique(\\[tas_document_id, unit_id\\])" packages/database/prisma/schema.prisma` returns one match.
    - `npx prisma validate` exits 0.
    - File `apps/api/src/rto/rto.service.ts` compiles unchanged (`npx tsc --noEmit` from `apps/api` still succeeds — verified in Task 5).
  </acceptance_criteria>
  <done>Schema expanded; validate + format pass; back-relations present on Rto, Qualification, User, Unit; TasDocumentUnit junction created.</done>
</task>

<task type="auto">
  <name>Task 4: Document S3 environment variables</name>
  <files>apps/api/.env.example, apps/api/.env</files>
  <read_first>
    - apps/api/.env.example (current contents)
    - apps/api/.env (current contents, if present)
    - .planning/phases/03-qualifications-units-tas-management/03-RESEARCH.md (Common Pitfalls section Pitfall 3)
  </read_first>
  <action>
    Append the following block to `apps/api/.env.example` (preserving existing content; add a leading blank line then a `# S3 / TAS Storage` comment header):

    ```
    S3_REGION=us-east-1
    S3_ENDPOINT=
    S3_ACCESS_KEY_ID=
    S3_SECRET_ACCESS_KEY=
    S3_BUCKET=trainsmart-tas-documents
    ```

    Then in `apps/api/.env`, append the same five keys with empty values (or copy from `.env.example` if `.env` does not exist yet — match Phase 1 convention). Do NOT commit real credentials. `S3_ENDPOINT` left empty selects AWS S3; setting it (e.g. `http://localhost:9000`) selects MinIO with `forcePathStyle: true`.
  </action>
  <verify>
    <automated>grep -c "^S3_BUCKET=" apps/api/.env.example apps/api/.env</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' apps/api/.env.example | grep -c '^S3_BUCKET='` returns 1.
    - `grep -v '^#' apps/api/.env.example | grep -c '^S3_ACCESS_KEY_ID='` returns 1.
    - `grep -v '^#' apps/api/.env.example | grep -c '^S3_SECRET_ACCESS_KEY='` returns 1.
    - `grep -v '^#' apps/api/.env.example | grep -c '^S3_REGION='` returns 1.
    - `grep -v '^#' apps/api/.env.example | grep -c '^S3_ENDPOINT='` returns 1.
    - `apps/api/.env` contains the same five keys.
    - No real secret value (any value matching `AKIA[0-9A-Z]{16}`) is committed.
  </acceptance_criteria>
  <done>Both env files document all five S3 variables.</done>
</task>

<task type="auto">
  <name>Task 5: [BLOCKING] Push schema to Neon and regenerate Prisma client</name>
  <files>packages/database/generated/client/, apps/api/dist/ (regeneration only)</files>
  <read_first>
    - packages/database/package.json (to confirm Prisma scripts)
    - packages/database/prisma/schema.prisma (modified file from Task 3)
    - apps/api/src/rto/rto.service.ts (to verify rebuild compiles)
  </read_first>
  <action>
    From `packages/database/`, run `npx prisma db push` against the live Neon DATABASE_URL. The new `TasDocument` columns are all nullable or defaulted so this should NOT trigger interactive prompts; if it does, fall back to `npx prisma db push --accept-data-loss` (TasDocument is currently empty in dev, no production data loss possible). After push, run `npx prisma generate` to regenerate the client. Then from `apps/api`, run `npm run build` (or `tsc --noEmit`) to confirm the placeholder `prisma.tasDocument.create({ data: { rto_id: rto.id } })` in `rto.service.ts` still type-checks against the new client.
  </action>
  <verify>
    <automated>cd packages/database && npx prisma db push && npx prisma generate && cd ../../apps/api && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0.
    - `npx prisma generate` exits 0; `packages/database/generated/client/index.d.ts` contains the string `TasDocumentUnit` and `qualification_id` within the `TasDocument` type.
    - `npx tsc --noEmit` in `apps/api` exits 0 (existing code still compiles against new client).
    - Live Neon DB has tables `tas_documents` with the new columns and `tas_document_units` (verify via Prisma Studio or `\d` query — can be checked manually by running `npx prisma studio` if needed).
  </acceptance_criteria>
  <done>Schema deployed to Neon; client regenerated; apps/api still compiles.</done>
</task>

</tasks>

<verification>
- Manual sanity check: open `packages/database/generated/client/index.d.ts` and search for `TasDocumentUnit` — type must exist.
- Confirm `apps/api` builds cleanly with `npx tsc --noEmit -p apps/api/tsconfig.json`.
- Confirm `apps/web` `package.json` lists Radix Select + Collapsible at the expected versions.
</verification>

<success_criteria>
- Expanded `TasDocument` model with all D-13 columns visible in `schema.prisma` and live DB.
- `TasDocumentUnit` junction table created with unique pair constraint.
- AWS SDK v3 packages installed in `apps/api`; Radix UI primitives installed in `apps/web`.
- `apps/api/.env.example` documents all five S3 variables.
- `npx prisma db push` applied; `apps/api` still type-checks.
</success_criteria>

<output>
Create `.planning/phases/03-qualifications-units-tas-management/03-01-SUMMARY.md` capturing: final field list on TasDocument, the chosen nullability strategy, any deviations from the proposed schema, and confirmation that downstream Plans 02–05 may now import from `@repo/db` types that include `tas_documents` and `TasDocumentUnit`.
</output>
