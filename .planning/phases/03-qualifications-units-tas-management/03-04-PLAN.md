---
phase: 03-qualifications-units-tas-management
plan: 04
type: execute
wave: 4
depends_on: ["03-02"]
files_modified:
  - apps/web/__tests__/qualification-detail.test.tsx
  - apps/web/__tests__/unit-detail.test.tsx
  - apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx
  - apps/web/app/(dashboard)/rto/[id]/units/[unitId]/page.tsx
  - apps/web/components/qualifications/QualificationsTab.tsx
  - apps/web/components/qualifications/QualificationDetail.tsx
  - apps/web/components/units/UnitDetail.tsx
  - apps/web/components/units/UnitSnapshotTimeline.tsx
  - apps/web/lib/api.ts
autonomous: true
requirements: [QUAL-03, QUAL-04, QUAL-05]
tags: [frontend, nextjs, qualification, unit, snapshot, tanstack-query]

must_haves:
  truths:
    - "Clicking a row in QualificationsTab navigates the user to /rto/{rtoId}/qualifications/{qualId}"
    - "Qualification detail page renders stacked sections in CONTEXT D-04 order: Qualification Info, Units, TAS Documents (placeholder slot — Plan 05 will hydrate), Trainers placeholder ('Coming in Phase 4'), Documents placeholder ('Coming in Phase 6')"
    - "Each unit row on the qualification detail page is a Link to /rto/{rtoId}/units/{unitId}"
    - "Unit detail page renders elements ordered by element_num with their performance criteria (numbered list)"
    - "Unit snapshot timeline lists snapshots most-recent-first; clicking a snapshot expands/collapses its element + PC wording (only one open at a time)"
    - "Breadcrumb on qualification detail: ← Back / RTO Name / Qualifications / {Qual Code}; on unit detail: + Units / {Unit Code}"
    - "Loading skeleton renders while data is pending; error message renders on fetch failure"
  artifacts:
    - path: "apps/web/__tests__/qualification-detail.test.tsx"
      provides: "Vitest smoke test stub for QualificationDetail page"
      min_lines: 10
    - path: "apps/web/__tests__/unit-detail.test.tsx"
      provides: "Vitest smoke test stub for UnitDetail page"
      min_lines: 10
    - path: "apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx"
      provides: "Qualification detail route"
      min_lines: 15
    - path: "apps/web/app/(dashboard)/rto/[id]/units/[unitId]/page.tsx"
      provides: "Unit detail route"
      min_lines: 15
    - path: "apps/web/components/qualifications/QualificationDetail.tsx"
      provides: "Stacked-section qualification detail component"
      exports: ["QualificationDetail"]
    - path: "apps/web/components/units/UnitDetail.tsx"
      provides: "Unit detail with elements + PCs"
      exports: ["UnitDetail"]
    - path: "apps/web/components/units/UnitSnapshotTimeline.tsx"
      provides: "Collapsible snapshot timeline"
      exports: ["UnitSnapshotTimeline"]
  key_links:
    - from: "apps/web/components/qualifications/QualificationsTab.tsx"
      to: "/rto/[id]/qualifications/[qualId]"
      via: "useRouter().push or Link wrapping row"
      pattern: "/qualifications/"
    - from: "QualificationDetail"
      to: "/qualifications/:qualId"
      via: "apiFetch via TanStack Query"
      pattern: "useQuery"
    - from: "UnitDetail"
      to: "/units/:unitId"
      via: "apiFetch via TanStack Query"
      pattern: "useQuery"
---

<objective>
Frontend detail pages for qualifications (D-01, QUAL-03) and units (D-02, QUAL-04, QUAL-05) plus the clickable-row affordance on the qualifications list (D-03). Includes the historical snapshot timeline (D-10, D-11) with one-at-a-time expand/collapse behaviour. UI follows the contract in 03-UI-SPEC.md (token-driven Tailwind classes, no shadcn CLI, manual Radix primitives).

Purpose: Without these pages the qualification rows go nowhere; phase success criteria #1 and #2 are unverifiable.
Output: Two Next.js App Router pages, three React components, lib/api helpers, a modification to QualificationsTab to make rows clickable, and Vitest smoke test stubs that will pass once the components are built.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-qualifications-units-tas-management/03-CONTEXT.md
@.planning/phases/03-qualifications-units-tas-management/03-UI-SPEC.md
@.planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md
@.planning/phases/03-qualifications-units-tas-management/03-02-SUMMARY.md
@apps/web/components/qualifications/QualificationsTab.tsx
@apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx
@apps/web/lib/api.ts
@apps/web/components/qualifications/ImportQualificationModal.tsx
@apps/web/components/rto/rto-workspace-header.tsx

<interfaces>
API endpoint contracts (from Plan 02 SUMMARY):
- `GET /qualifications/:qualId` returns:
  ```
  {
    id, code, title, status, superseded_by, training_package, last_synced_at,
    units: [{ id, qualification_id, unit_id, created_at, unit: { id, code, title, status, superseded_by } }],
    tasDocuments: [{ id, version_label, status, file_name, review_date, created_at, ... }]
  }
  ```
- `GET /units/:unitId` returns:
  ```
  {
    id, code, title, status, superseded_by,
    elements: [{ id, element_num, title, performance_criteria: [{ id, pc_num, text }] }],
    snapshots: [{ id, snapshotted_at, change_fields, snapshot_data }]
  }
  ```

`UnitSnapshot.snapshot_data` shape (from PATTERNS.md, confirmed in tga-sync.service.ts):
`{ ...unit, elements: [{ element_num, title, performance_criteria: [{ pc_num, text }] }] }`. Snake_case throughout.

Existing `apiFetch` from `apps/web/lib/api.ts` lines 33–77 — handles auth refresh, returns parsed JSON.

Existing TanStack Query QueryClient — already registered in providers in Phase 1. Use `useQuery({ queryKey: [...], queryFn: () => apiFetch(...) })`.

UI tokens from `03-UI-SPEC.md`: container `px-8 py-6`, headings `text-2xl font-semibold leading-tight`, section headings `text-xl font-semibold mb-4`, body `text-sm`, mono `font-mono text-sm` for codes, badge `px-2 py-1 rounded text-xs font-medium`.

Status badge colors (UI-SPEC Status Badges table):
- Current → green-100/800
- Superseded → red-100/800
- Draft → amber-100/800
- Archived → muted

Placeholder block (UI-SPEC): `<div className="rounded-lg border border-dashed border-border bg-muted/50 p-8 text-center"><p className="text-sm text-muted-foreground">{copy}</p></div>`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser route params | `[id]`, `[qualId]`, `[unitId]` are user-controlled URL segments |
| Fetch response | API responses are trusted (same-origin authenticated calls) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | URL param qualId/unitId | accept | Backend validates UUID and returns 404 (Plan 02). Frontend renders error state for non-200 responses. |
| T-03-11 | Information Disclosure | snapshot_data rendering | accept | snapshot_data is non-sensitive TGA wording; React's default escaping prevents XSS from any embedded user content. No `dangerouslySetInnerHTML` used. |
| T-03-12 | Spoofing | Detail page accessed without auth | mitigate | apiFetch refreshes auth via existing Phase 1 middleware; unauthenticated users redirected to login |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Create Vitest smoke test stubs for detail pages</name>
  <files>apps/web/__tests__/qualification-detail.test.tsx, apps/web/__tests__/unit-detail.test.tsx</files>
  <read_first>
    - apps/web/package.json (confirm vitest and @testing-library/react are present; if not, note for SUMMARY)
    - apps/web/vitest.config.ts or apps/web/vite.config.ts (confirm test setup file and jsdom environment)
  </read_first>
  <action>
    Create `apps/web/__tests__/qualification-detail.test.tsx` as a skeleton smoke test. The component files it imports do not exist yet — that is intentional. The stub establishes the test contract that Tasks 1–3 must satisfy.

    The file must:
    - Import `{ render, screen }` from `@testing-library/react`.
    - Import `QualificationDetail` from `@/components/qualifications/QualificationDetail` (will fail to resolve until Task 2).
    - Wrap the component in a minimal QueryClientProvider using `new QueryClient({ defaultOptions: { queries: { retry: false } } })` imported from `@tanstack/react-query`.
    - Contain one test: `it('renders without crashing', () => { render(...); })` — does not assert any specific text; existence of the test file is the contract.
    - The test is EXPECTED to fail at this point (import cannot resolve). Do not add `// @ts-ignore` or skip markers — the test legitimately fails until the component exists.

    Create `apps/web/__tests__/unit-detail.test.tsx` following the identical pattern:
    - Import `UnitDetail` from `@/components/units/UnitDetail`.
    - Same QueryClientProvider wrapper.
    - One test: `it('renders without crashing', () => { render(...); })`.

    Both files must be real `.tsx` files with valid TypeScript syntax (aside from the unresolved import). Do not use `.js` or `.jsx`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --allowImportingTsExtensions 2>&1 | grep -v "Cannot find module" | grep -c "error TS" || true</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/__tests__/qualification-detail.test.tsx` exists and contains the string `QualificationDetail`.
    - `apps/web/__tests__/unit-detail.test.tsx` exists and contains the string `UnitDetail`.
    - Both files contain `renders without crashing` test description.
    - Both files use `QueryClientProvider` wrapper.
    - Running `pnpm --filter web test -- --testPathPattern qualification-detail` exits non-zero (import unresolved) — this is the RED state and is expected.
  </acceptance_criteria>
  <done>Vitest stub files created; test suite is in RED state awaiting component implementation in Tasks 1–3.</done>
</task>

<task type="auto">
  <name>Task 1: Make qualifications table rows clickable + add API helpers</name>
  <files>apps/web/components/qualifications/QualificationsTab.tsx, apps/web/lib/api.ts</files>
  <read_first>
    - apps/web/components/qualifications/QualificationsTab.tsx (current row structure, lines 83–110)
    - apps/web/lib/api.ts (existing helper pattern, lines 1–77)
    - .planning/phases/03-qualifications-units-tas-management/03-UI-SPEC.md (Interaction Contracts > Qualification List Rows)
    - .planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md (Pattern 5 + Clickable Table Rows section)
  </read_first>
  <action>
    In `apps/web/components/qualifications/QualificationsTab.tsx`:
    1. Add `import { useRouter } from 'next/navigation';` at the top.
    2. Inside the component, add `const router = useRouter();` near the existing hook calls.
    3. Modify the `<tr>` element (currently at lines ~83–86) to add `cursor-pointer` to its className and `onClick={() => router.push(\`/rto/${rtoId}/qualifications/${rq.qualification?.id}\`)}` handler.
    4. Add a 5th column: in the `<thead>` row append `<th className="w-8"></th>` (or empty `<th>`), and in each `<tr>` append `<td><ChevronRight className="h-4 w-4 text-muted-foreground" /></td>`. Import `ChevronRight` from `lucide-react`.
    5. Add `role="link"`, `tabIndex={0}`, and `onKeyDown` Enter handler on the `<tr>` (per UI-SPEC accessibility requirements). Keyboard handler triggers the same `router.push(...)`.
    6. Guard against `rq.qualification?.id` being undefined — if undefined, render the row without `cursor-pointer` or onClick (defensive).

    In `apps/web/lib/api.ts`, append the helpers at the bottom (after existing exports, matching the existing named-export style):
    ```
    export async function getQualificationDetail(qualId: string) {
      return apiFetch(`/qualifications/${qualId}`);
    }
    export async function getUnitDetail(unitId: string) {
      return apiFetch(`/units/${unitId}`);
    }
    ```
    Do not modify existing exports.
  </action>
  <verify>
    <automated>cd apps/web && npm run lint && npx tsc --noEmit && pnpm --filter web test -- --testPathPattern qualification-detail 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useRouter" apps/web/components/qualifications/QualificationsTab.tsx` returns at least one match.
    - `grep -n "cursor-pointer" apps/web/components/qualifications/QualificationsTab.tsx` returns at least one match on the row.
    - `grep -n "/qualifications/\\$\\{" apps/web/components/qualifications/QualificationsTab.tsx` returns at least one match (push URL template).
    - `grep -n "ChevronRight" apps/web/components/qualifications/QualificationsTab.tsx` returns at least one match.
    - `grep -n "role=\"link\"" apps/web/components/qualifications/QualificationsTab.tsx` returns at least one match.
    - `grep -n "export async function getQualificationDetail" apps/web/lib/api.ts` returns one match.
    - `grep -n "export async function getUnitDetail" apps/web/lib/api.ts` returns one match.
    - `npx tsc --noEmit` in `apps/web` exits 0.
  </acceptance_criteria>
  <done>Rows are clickable, keyboard-accessible, and API helpers exported.</done>
</task>

<task type="auto">
  <name>Task 2: Build QualificationDetail component + Next.js route</name>
  <files>apps/web/components/qualifications/QualificationDetail.tsx, apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx</files>
  <read_first>
    - apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx (server-component params Promise pattern)
    - apps/web/components/qualifications/QualificationsTab.tsx (table + loading/error patterns)
    - apps/web/components/rto/rto-workspace-header.tsx (breadcrumb pattern)
    - .planning/phases/03-qualifications-units-tas-management/03-UI-SPEC.md (Page Layouts > Qualification Detail Page)
    - .planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md (QualificationDetail.tsx section)
  </read_first>
  <action>
    Create `apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx` as a server component:
    - `export default async function QualificationDetailPage({ params }: { params: Promise<{ id: string; qualId: string }> })`
    - Await params, destructure `{ id, qualId }`.
    - Return `<div className="px-8 py-6"><QualificationDetail rtoId={id} qualId={qualId} /></div>`.
    - Add `import { QualificationDetail } from '@/components/qualifications/QualificationDetail';`

    Create `apps/web/components/qualifications/QualificationDetail.tsx` as a `'use client'` component:
    - Props: `{ rtoId: string; qualId: string }`.
    - `useQuery({ queryKey: ['qualification', qualId], queryFn: () => apiFetch(`/qualifications/${qualId}`) })`.
    - Render the breadcrumb row per UI-SPEC: `<nav aria-label="Breadcrumb" className="flex items-center gap-3 mb-6 text-sm">` containing the back link to `/rto/${rtoId}/qualifications` ("← Back"), the qualifications list link, then the current Qual Code in `text-foreground font-medium`.
    - Loading state: render the skeleton block from UI-SPEC (h-6 w-48, h-8 w-96 mt-4, h-4 w-64 mt-2, three h-12 rounded-lg border border-border mt-6, two more for TAS slot). Wrap container with `aria-busy="true"`.
    - Error state: `<p className="text-sm text-destructive">Could not load qualification. Refresh the page to try again.</p>`.
    - Success state — sections in D-04 order:
      1. Heading: `<h1 className="text-2xl font-semibold leading-tight">{title}</h1>` then `<div className="mt-1 flex items-center gap-2">{code in font-mono text-sm} + status badge (Current=green, Superseded=red)</div>` + metadata row `<div className="mt-2 text-sm text-muted-foreground flex gap-6">Training Package: {training_package} · Last Synced: {formatted date} · {Superseded by: {superseded_by} if present}</div>`.
      2. Units section (`mt-8`): `<h2 className="text-xl font-semibold mb-4">Units</h2>`. Render table mirroring QualificationsTab columns: Code | Title | Status | (chevron). Each row is `<Link href={`/rto/${rtoId}/units/${u.unit.id}`}>` wrapping `<tr>` content — use `router.push` onClick (consistent with Task 1). Apply `min-h-[44px]` per UI-SPEC. Empty state copy: "No units linked to this qualification."
      3. TAS Documents section (`mt-8`): heading `<h2>` + "Upload TAS" button (right-aligned). Below: `<div data-tas-version-list-slot data-rto-id={rtoId} data-qual-id={qualId}></div>` — Plan 05 will hydrate this slot with TasVersionList. For now, render an empty list container with copy "No TAS documents uploaded yet." using `qualification.tasDocuments` length. The actual TasVersionList component is delivered in Plan 05; this plan only renders the placeholder slot + empty/heading structure.
      4. Trainers section (`mt-8`): placeholder block `<div className="rounded-lg border border-dashed border-border bg-muted/50 p-8 text-center"><p className="text-sm text-muted-foreground">Trainer mapping will be available in Phase 4.</p></div>` (per D-05 + UI-SPEC).
      5. Documents section (`mt-8`): same placeholder pattern with copy "Document management will be available in Phase 6."

    All className strings copied verbatim from UI-SPEC. Use the status badge color map per UI-SPEC table.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run build && pnpm --filter web test -- --testPathPattern qualification-detail</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx` exists and exports default async function.
    - `grep -n "await params" apps/web/app/(dashboard)/rto/[id]/qualifications/[qualId]/page.tsx` returns one match.
    - `grep -n "useQuery" apps/web/components/qualifications/QualificationDetail.tsx` returns one match.
    - `grep -n "queryKey: \\['qualification'" apps/web/components/qualifications/QualificationDetail.tsx` returns one match.
    - `grep -n "Trainer mapping will be available in Phase 4" apps/web/components/qualifications/QualificationDetail.tsx` returns one match.
    - `grep -n "Document management will be available in Phase 6" apps/web/components/qualifications/QualificationDetail.tsx` returns one match.
    - `grep -n "text-2xl font-semibold" apps/web/components/qualifications/QualificationDetail.tsx` returns at least one match (heading).
    - `pnpm --filter web test -- --testPathPattern qualification-detail` exits 0 (smoke test passes now that component exists).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Qualification detail route + component render all five sections per UI-SPEC; Vitest smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Build UnitDetail + UnitSnapshotTimeline + Next.js route</name>
  <files>apps/web/app/(dashboard)/rto/[id]/units/[unitId]/page.tsx, apps/web/components/units/UnitDetail.tsx, apps/web/components/units/UnitSnapshotTimeline.tsx</files>
  <read_first>
    - apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx (server component pattern)
    - apps/web/components/qualifications/QualificationsTab.tsx (TanStack Query + loading/error)
    - .planning/phases/03-qualifications-units-tas-management/03-UI-SPEC.md (Unit Detail Page + Historical Snapshot Timeline sections)
    - .planning/phases/03-qualifications-units-tas-management/03-PATTERNS.md (UnitDetail + UnitSnapshotTimeline sections, snapshot_data field names list)
    - apps/web/components/qualifications/ImportQualificationModal.tsx (useState toggle pattern for collapse)
  </read_first>
  <action>
    Create `apps/web/app/(dashboard)/rto/[id]/units/[unitId]/page.tsx`:
    - `export default async function UnitDetailPage({ params }: { params: Promise<{ id: string; unitId: string }> })`
    - Awaits params, destructures `{ id, unitId }`.
    - Returns `<div className="px-8 py-6"><UnitDetail rtoId={id} unitId={unitId} /></div>`.

    Create `apps/web/components/units/UnitDetail.tsx` (`'use client'`):
    - Props: `{ rtoId: string; unitId: string }`.
    - `useQuery({ queryKey: ['unit', unitId], queryFn: () => apiFetch(`/units/${unitId}`) })`.
    - Breadcrumb per UI-SPEC: `← Back / RTO Name / Qualifications / Units / {Unit Code}`. The "← Back" link points to the qualification detail page — read the parent qualification from the unit's relations is NOT available on this endpoint, so the back link points to `/rto/${rtoId}/qualifications` for simplicity. RTO Name is unavailable in this fetch; render RTO id segment as `RTO` placeholder text, or fetch the RTO name with a second `useQuery({ queryKey: ['rto', rtoId], queryFn: () => apiFetch(`/rtos/${rtoId}`) })` if the existing endpoint supports it (verified by reading rto-workspace-header.tsx — if it uses `apiFetch('/rtos/${rtoId}')` to get the name, do the same).
    - Loading skeleton per UI-SPEC Unit detail skeleton.
    - Error state: "Could not load unit. Refresh the page to try again."
    - Success state:
      1. Heading: title + code (font-mono) + status badge.
      2. Metadata row: `Superseded by: {code}` only if present.
      3. Elements section (`mt-8`): `<h2 className="text-xl font-semibold mb-4">Elements &amp; Performance Criteria</h2>`. For each element (already ordered by element_num asc from API), render a card: `<div className="bg-card p-4 rounded-lg border border-border space-y-3"><h3 className="text-base font-semibold leading-tight">Element {element_num}: {element.title}</h3>` then a list of PCs `<div className="flex items-start gap-2 py-2"><span className="font-mono text-xs text-muted-foreground w-8 shrink-0">{pc_num}</span><p className="text-sm text-foreground">{text}</p></div>`. Elements are spaced with `space-y-3`. Use `key={element.id}` and `key={pc.id}`.
      4. Historical Snapshots section (`mt-8`): heading + count badge in muted. Render `<UnitSnapshotTimeline snapshots={unit.snapshots} />`.

    Create `apps/web/components/units/UnitSnapshotTimeline.tsx` (`'use client'`):
    - Props: `{ snapshots: Array<{ id: string; snapshotted_at: string; change_fields: string[]; snapshot_data: any }> }`.
    - State: `const [openId, setOpenId] = useState<string | null>(null);` — only one open at a time per UI-SPEC interaction contract.
    - If `snapshots.length === 0`: render empty state per UI-SPEC ("No historical snapshots recorded yet.").
    - Otherwise render a `<div className="space-y-2">` container. For each snapshot (assumed already ordered desc by API):
      - Trigger row: `<button type="button" aria-expanded={openId === s.id} aria-controls={`snapshot-${s.id}`} onClick={() => setOpenId(openId === s.id ? null : s.id)} className="w-full flex items-center gap-2 px-4 py-3 rounded-lg border border-border bg-card cursor-pointer hover:bg-muted/50 transition-colors text-sm">`.
      - Inside trigger: ChevronRight icon with `className={\`h-4 w-4 text-muted-foreground transition-transform duration-200 ${openId === s.id ? 'rotate-90' : ''}\`}`, date span `<span className="font-medium text-foreground">{formatDate(s.snapshotted_at, 'DD MMM YYYY')}</span>`, change summary `<span className="text-muted-foreground">{change_fields.join(', ')}</span>` if present.
      - When expanded: `<div id={`snapshot-${s.id}`} className="mt-1 px-4 pb-4 pt-2 rounded-b-lg border-x border-b border-border bg-card">`. Inside, iterate `snapshot_data.elements` (snake_case field names per PATTERNS.md), rendering each element header `Element {element_num}: {title}` then nested `performance_criteria` list using `pc_num` and `text`. Use `text-sm text-muted-foreground` for the wording to visually subdue snapshot content (per UI-SPEC).
    - Defensive: if `s.snapshot_data?.elements` is not an array, render `<p className="text-sm text-muted-foreground">Snapshot data unavailable.</p>`.
    - Date formatting: use `new Date(s.snapshotted_at).toLocaleDateString('en-AU', { day: '2-digit', month: 'short', year: 'numeric' })` (no extra library required).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run build && pnpm --filter web test -- --testPathPattern unit-detail</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/app/(dashboard)/rto/[id]/units/[unitId]/page.tsx` exists.
    - `grep -n "queryKey: \\['unit'" apps/web/components/units/UnitDetail.tsx` returns one match.
    - `grep -n "Elements &amp; Performance Criteria\\|Elements & Performance Criteria" apps/web/components/units/UnitDetail.tsx` returns at least one match.
    - `grep -n "element_num" apps/web/components/units/UnitDetail.tsx` returns at least one match.
    - `grep -n "performance_criteria" apps/web/components/units/UnitDetail.tsx` returns at least one match.
    - `grep -n "UnitSnapshotTimeline" apps/web/components/units/UnitDetail.tsx` returns at least one match.
    - `grep -n "aria-expanded" apps/web/components/units/UnitSnapshotTimeline.tsx` returns one match.
    - `grep -n "setOpenId" apps/web/components/units/UnitSnapshotTimeline.tsx` returns at least one match.
    - `grep -n "rotate-90" apps/web/components/units/UnitSnapshotTimeline.tsx` returns one match.
    - `grep -n "snapshot_data.elements\\|snapshot_data\\?.elements" apps/web/components/units/UnitSnapshotTimeline.tsx` returns at least one match.
    - `pnpm --filter web test -- --testPathPattern unit-detail` exits 0.
    - `npm run build` in `apps/web` exits 0.
  </acceptance_criteria>
  <done>Unit detail + snapshot timeline render; only one snapshot can be open at a time; Vitest smoke test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of detail pages</name>
  <read_first>
    - .planning/phases/03-qualifications-units-tas-management/03-UI-SPEC.md (full Page Layouts section)
  </read_first>
  <what-built>
    - QualificationsTab rows are now clickable links to qualification detail pages
    - Qualification detail page at /rto/{id}/qualifications/{qualId} with five sections (Info, Units, TAS placeholder, Trainers placeholder, Documents placeholder)
    - Unit detail page at /rto/{id}/units/{unitId} with Elements/PCs and Historical Snapshots timeline
  </what-built>
  <how-to-verify>
    1. Start dev servers (api + web). Sign in as a known user.
    2. Open any RTO that has at least one imported qualification. Click a qualification row — URL should navigate to `/rto/{id}/qualifications/{qualId}`.
    3. Confirm the qualification detail page shows: title, code (mono), status badge, training package, last synced, units table, TAS Documents header (empty list — OK for now), and two dashed placeholder blocks reading "Trainer mapping will be available in Phase 4" and "Document management will be available in Phase 6".
    4. Click a unit row → URL navigates to `/rto/{id}/units/{unitId}`. Confirm: title, code, status badge, list of elements with numbered PCs, and Historical Snapshots section. If the unit has snapshots, click a snapshot — it expands to show element wording at that point in time. Click another snapshot — the first collapses (only one open at a time). Click the same snapshot again — it collapses.
    5. Press Tab to navigate to a qualification row, press Enter — should navigate (keyboard accessibility).
    6. Refresh the qualification detail page directly via URL — page loads without redirect (shareable URLs per D-01).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issue.</resume-signal>
  <acceptance_criteria>
    - User confirms all steps pass.
    - No console errors during navigation.
    - Placeholder copy exactly matches UI-SPEC.
  </acceptance_criteria>
  <done>User approves detail pages.</done>
</task>

</tasks>

<verification>
- Frontend build: `cd apps/web && npm run build`.
- Lint: `npm run lint`.
- Type check: `npx tsc --noEmit`.
- Vitest: `pnpm --filter web test -- --testPathPattern qualification-detail|unit-detail`.
</verification>

<success_criteria>
- Qualification list rows navigate to detail pages.
- Qualification detail page renders all five sections in D-04 order.
- Unit detail page renders elements with PCs and a working snapshot timeline.
- Snapshots expand/collapse one-at-a-time with keyboard accessibility.
- Shareable URLs work (direct refresh loads correctly).
- Vitest smoke tests for both detail pages pass.
</success_criteria>

<output>
Create `.planning/phases/03-qualifications-units-tas-management/03-04-SUMMARY.md` documenting: actual component file structure, any divergence from UI-SPEC (with rationale), how the snapshot date is formatted, confirmation that the TAS Documents slot is ready to be hydrated by Plan 05, and confirmation that Vitest stubs pass.
</output>
