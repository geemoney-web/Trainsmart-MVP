---
phase: 01-foundation-rto-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/nest-cli.json
  - apps/api/jest.config.ts
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/common/decorators/public.decorator.ts
  - apps/api/src/auth/auth.module.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/dto/login.dto.ts
  - apps/api/src/auth/strategies/access-token.strategy.ts
  - apps/api/src/auth/strategies/refresh-token.strategy.ts
  - apps/api/src/auth/guards/access-token.guard.ts
  - apps/api/src/auth/guards/refresh-token.guard.ts
  - apps/api/src/rto/rto.module.ts
  - apps/api/src/rto/rto.service.ts
  - apps/api/src/rto/rto.controller.ts
  - apps/api/src/rto/dto/create-rto.dto.ts
  - apps/api/src/rto/dto/update-rto.dto.ts
  - apps/api/src/rto/dto/rto-response.dto.ts
  - apps/api/src/users/users.module.ts
  - apps/api/src/users/users.service.ts
  - apps/api/test/auth.e2e-spec.ts
  - apps/api/test/rto.e2e-spec.ts
  - apps/api/test/jest-e2e.json
  - apps/api/.env.test
autonomous: false
requirements: [RTO-01, RTO-02, RTO-03, RTO-04, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06]
tags: [nestjs, jwt, auth, rto, api]

must_haves:
  truths:
    - "POST /api/v1/auth/login with valid credentials returns 200 + JSON { accessToken } AND sets httpOnly refresh_token cookie"
    - "POST /api/v1/auth/login with invalid credentials returns 401/403"
    - "POST /api/v1/rtos (with valid Bearer access token) creates an RTO and atomically inserts at least one stub row per workspace sub-entity (qualifications, trainers, tas_documents, validations, documents, tasks, notes — NOT compliance_alerts)"
    - "GET /api/v1/rtos (authenticated) returns RTOs with _count.alerts (unresolved, always 0 in Phase 1), _count.validations (upcoming), and static placeholder counts: superseded_quals_count=0, tas_review_count=0, trainer_alerts_count=0"
    - "Unauthenticated requests to /api/v1/rtos return 401"
    - "GET /api/v1/auth/login without @Public decorator on its handler would return 401; with @Public it returns its real response"
  artifacts:
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap with /api/v1 prefix, global AccessTokenGuard, helmet, cors+credentials, cookie-parser, ValidationPipe whitelist+forbidNonWhitelisted"
      contains: "setGlobalPrefix"
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "POST /auth/login (@Public), POST /auth/refresh (@UseGuards RefreshTokenGuard), POST /auth/logout"
      exports: ["AuthController"]
    - path: "apps/api/src/rto/rto.controller.ts"
      provides: "GET/POST/GET:id/PATCH:id/DELETE:id on /rtos"
      exports: ["RtoController"]
    - path: "apps/api/src/rto/rto.service.ts"
      provides: "findAll with soft-delete + _count include + static placeholder count fields, create inside prisma.$transaction provisioning stub rows (no ComplianceAlert), soft-delete remove"
      exports: ["RtoService"]
    - path: "apps/api/test/rto.e2e-spec.ts"
      provides: "E2E coverage for RTO-01, RTO-02, RTO-03, DASH-02, DASH-04, DASH-05, DASH-06 (placeholder counts)"
      contains: "describe('Rto"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/auth/guards/access-token.guard.ts"
      via: "app.useGlobalGuards(app.get(AccessTokenGuard))"
      pattern: "useGlobalGuards"
    - from: "apps/api/src/auth/guards/access-token.guard.ts"
      to: "apps/api/src/common/decorators/public.decorator.ts"
      via: "reflector.getAllAndOverride(IS_PUBLIC_KEY, ...)"
      pattern: "IS_PUBLIC_KEY"
    - from: "apps/api/src/rto/rto.service.ts"
      to: "@repo/db (packages/database)"
      via: "import { prisma } from '@repo/db'"
      pattern: "from '@repo/db'"
    - from: "apps/api/src/rto/rto.service.ts create()"
      to: "stub entity tables (rto_qualifications, trainers, tas_documents, validations, documents, tasks, notes — 7 tables, NOT compliance_alerts)"
      via: "prisma.$transaction provisioning per RTO-02"
      pattern: "\\$transaction"
---

<objective>
Build the NestJS API: in-house JWT auth (access 15m + refresh 7d) per D-05, full RTO CRUD per RTO-01..04, workspace auto-provisioning per RTO-02, and the dashboard aggregated counts endpoint that powers DASH-02, DASH-03, and the static placeholder counts for DASH-04, DASH-05, DASH-06.

Purpose: Phase 1's API surface. Every later phase mounts on this auth layer and adds modules alongside `RtoModule`. Threat model demands a global guard with a `@Public()` escape hatch (RESEARCH.md Pitfall 3), httpOnly refresh cookie (Pitfall 2), and `forbidNonWhitelisted` ValidationPipe to prevent mass assignment. DASH-04/05/06 placeholder count fields are hardcoded to 0 in Phase 1; Phase 5 (compliance engine) will compute real values.

Output: Running NestJS app at `:3001` with `/api/v1/*` prefix, all 5 RTO endpoints behind AccessTokenGuard, 3 auth endpoints, and Jest E2E tests covering RTO-01/02/03/04, DASH-02/03/04/05/06, and the D-05 auth flow.

NOTE on autonomous=false: This plan's E2E tests require a live PostgreSQL provisioned by the human-action checkpoint in Plan 01-01 (Task 0). Cannot run end-to-end without that prerequisite, hence `autonomous: false`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-rto-dashboard/01-CONTEXT.md
@.planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md
@.planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md
@packages/database/prisma/schema.prisma
@packages/database/src/index.ts

<interfaces>
<!-- @repo/db is created by Plan 01-01. Executor consumes these exports directly. -->

From packages/database/src/index.ts (Plan 01-01):
```typescript
export { prisma } from './client';     // singleton PrismaClient instance
export * from '../generated/client';   // model types (User, Rto, RtoQualification, ...), Prisma namespace
```

Key Prisma model shapes the API uses (from schema.prisma):
- User: { id, email, password_hash, full_name, refresh_token (nullable), created_at, updated_at, deleted_at }
- Rto: { id, name, asqa_code, operating_states (String[]), contact_name?, contact_email?, contact_phone?, status_color (default "gray"), timestamps, deleted_at, relations: alerts, validations, qualifications, trainers, tas_documents, documents, tasks, notes }
- ComplianceAlert: { id, rto_id, resolved (default false), timestamps, deleted_at } — NOT provisioned by RTO create in Phase 1; real alerts come from Phase 5 compliance engine
- Validation: { id, rto_id, planned_date?, status (default "Scheduled"), timestamps, deleted_at }
- Stub models all have `rto_id String @db.Uuid` and `@relation(fields: [rto_id], references: [id])` back to Rto.

RtoResponseDto (the shape returned by findAll/findOne):
```typescript
export class RtoResponseDto {
  id: string;
  name: string;
  asqa_code: string;
  operating_states: string[];
  contact_name?: string;
  contact_email?: string;
  contact_phone?: string;
  status_color: string;
  _count: { alerts: number; validations: number };
  // Static placeholder counts — hardcoded to 0 in Phase 1. Phase 5 (compliance engine) will compute real values.
  superseded_quals_count: number;  // DASH-04 placeholder
  tas_review_count: number;        // DASH-05 placeholder
  trainer_alerts_count: number;    // DASH-06 placeholder
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold NestJS app with deps, config, and DI wiring</name>
  <read_first>
    - .planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md (§apps/api/src/main.ts and all auth/* sections)
    - .planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md (Pattern 2, Pattern 3, Pitfalls 2/3/6/8, §Recommended Project Structure)
    - packages/database/src/index.ts (verify @repo/db export shape)
    - turbo.json (so executor knows how to plug into the build/dev/test pipeline)
  </read_first>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/nest-cli.json,
    apps/api/jest.config.ts,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/api/src/common/decorators/public.decorator.ts,
    apps/api/src/users/users.module.ts,
    apps/api/src/users/users.service.ts,
    apps/api/.env.test
  </files>
  <action>
    Create the `apps/api` NestJS workspace per D-01/D-03/D-04 and RESEARCH.md Recommended Project Structure.

    1. `apps/api/package.json` — name `@trainsmart/api`, scripts: `dev` (`nest start --watch`), `build` (`nest build`), `start` (`node dist/main`), `lint` (`eslint "src/**/*.ts"`), `test` (`jest`), `test:e2e` (`jest --config test/jest-e2e.json`). Dependencies per RESEARCH.md §Standard Stack:
       - `@nestjs/core@^11.1.21`, `@nestjs/common@^11.1.21`, `@nestjs/platform-express@^11.1.21`, `@nestjs/passport@^11.0.5`, `@nestjs/jwt@^11.0.2`, `@nestjs/config@^4.0.4`, `@nestjs/swagger@^11.4.3`
       - `passport@latest`, `passport-jwt@^4.0.1`, `bcryptjs@^3.0.3`
       - `class-validator@^0.15.1`, `class-transformer@^0.5.1`
       - `helmet@^8.1.0`, `cookie-parser@^1.4.7`, `cors@^2.8.6`
       - `reflect-metadata@latest`, `rxjs@^7.8.0`
       - `@repo/db@workspace:*` (resolves to packages/database via pnpm workspace protocol)
       DevDependencies: `@nestjs/cli@latest`, `@nestjs/testing@^11.1.21`, `@types/express`, `@types/node`, `@types/passport-jwt`, `@types/bcryptjs`, `@types/cookie-parser`, `@types/supertest`, `jest@^29`, `ts-jest@^29`, `supertest@^7`, `typescript@^5.6`, `ts-node@latest`.

    2. `apps/api/tsconfig.json` — `target: ES2022`, `module: commonjs`, `moduleResolution: node`, `experimentalDecorators: true`, `emitDecoratorMetadata: true` (CRITICAL per RESEARCH.md Pitfall 6), `strict: true`, `outDir: dist`, `baseUrl: .`, `paths` mapping `"@repo/db": ["../../packages/database/src/index.ts"]` so dev TypeScript sees the workspace.

    3. `apps/api/nest-cli.json` — `{ "collection": "@nestjs/schematics", "sourceRoot": "src", "compilerOptions": { "deleteOutDir": true } }`.

    4. `apps/api/jest.config.ts` — preset `ts-jest`, testEnvironment `node`, rootDir `src`, testRegex `.*\\.spec\\.ts$`, moduleNameMapper for `@repo/db`.

    5. `apps/api/src/main.ts` — copy verbatim from PATTERNS.md §apps/api/src/main.ts. First line MUST be `import 'reflect-metadata';` (Pitfall 6). Sets `/api/v1` prefix; mounts `helmet()`, `cookieParser()`, `cors({ origin: process.env.FRONTEND_URL, credentials: true })` (Pitfall 2); uses `app.useGlobalGuards(app.get(AccessTokenGuard))` (Pitfall 3); applies `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true })`. Listens on port 3001.

    6. `apps/api/src/app.module.ts` — root module. Imports: `ConfigModule.forRoot({ isGlobal: true })`, `AuthModule`, `RtoModule`, `UsersModule`. No controllers/providers at this level.

    7. `apps/api/src/common/decorators/public.decorator.ts` — copy verbatim from PATTERNS.md §apps/api/src/common/decorators/public.decorator.ts. Exports `IS_PUBLIC_KEY` constant and `Public()` decorator factory using `SetMetadata`.

    8. `apps/api/src/users/users.module.ts` and `users.service.ts` — minimal `UsersService` exporting `findByEmail(email: string)` and `findById(id: string)` methods, both filtered by `deleted_at: null`. `UsersModule` declares the provider and exports it for `AuthModule`.

    9. `apps/api/.env.test` — `DATABASE_URL` pointing at a separate test database name (`trainsmart_test`), `JWT_ACCESS_SECRET=test-access-secret-min-32-characters-long-aaaa`, `JWT_REFRESH_SECRET=test-refresh-secret-min-32-characters-long-bbbb`, `FRONTEND_URL=http://localhost:3000`, `NODE_ENV=test`.

    Run `pnpm install` after creating files so the workspace resolves @repo/db and downloads NestJS deps. Confirm `pnpm --filter @trainsmart/api exec nest --version` works.

    Do NOT scaffold via `nest new` (it overwrites turbo.json and package.json defaults) — create files explicitly.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path apps/api/src/main.ts) -and (Test-Path apps/api/src/app.module.ts) -and (Test-Path apps/api/src/common/decorators/public.decorator.ts) -and (Select-String -Path apps/api/src/main.ts -Pattern \"setGlobalPrefix\\('api/v1'\\)\" -Quiet) -and (Select-String -Path apps/api/src/main.ts -Pattern 'useGlobalGuards' -Quiet) -and (Select-String -Path apps/api/src/main.ts -Pattern 'reflect-metadata' -Quiet) -and (Select-String -Path apps/api/src/main.ts -Pattern 'credentials: true' -Quiet)) { exit 0 } else { exit 1 }"; pnpm --filter @trainsmart/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/main.ts` first non-comment line is `import 'reflect-metadata';`
    - `apps/api/src/main.ts` contains all of: `setGlobalPrefix('api/v1')`, `cookieParser()`, `helmet()`, `enableCors({` with `credentials: true`, `useGlobalGuards(app.get(AccessTokenGuard))`, `ValidationPipe({` with `whitelist: true` AND `forbidNonWhitelisted: true`
    - `apps/api/src/common/decorators/public.decorator.ts` exports `IS_PUBLIC_KEY` and `Public`
    - `apps/api/package.json` declares dependency `@repo/db: "workspace:*"`
    - `apps/api/tsconfig.json` sets both `experimentalDecorators: true` AND `emitDecoratorMetadata: true`
    - `pnpm --filter @trainsmart/api exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    NestJS app scaffold compiles, root module wired, `@Public()` decorator available, `@repo/db` reachable from API source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AuthModule (JWT two-strategy login/refresh/logout) + auth E2E tests</name>
  <read_first>
    - .planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md (§auth strategies, guards, service, controller)
    - .planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md (Pattern 2, Pitfalls 2/3, §Security Domain)
    - apps/api/src/common/decorators/public.decorator.ts
    - apps/api/src/main.ts
    - apps/api/src/users/users.service.ts
    - packages/database/prisma/schema.prisma (User model fields)
  </read_first>
  <behavior>
    - Test: POST /api/v1/auth/login with seeded user (email/password) returns 200 with JSON body `{ accessToken: string }` AND `set-cookie` header containing `refresh_token=...; HttpOnly`
    - Test: POST /api/v1/auth/login with wrong password returns 401 or 403
    - Test: POST /api/v1/auth/login with non-existent email returns 401 or 403 (must not leak which one is wrong)
    - Test: GET /api/v1/rtos without Authorization header returns 401 (verifies global guard wired)
    - Test: GET /api/v1/rtos with valid Bearer accessToken returns 200 (verifies @Public escape hatch works on /auth/login but everything else is protected)
    - Test: POST /api/v1/auth/refresh with Bearer refreshToken returns new accessToken AND rotates refresh_token in DB
    - Test: POST /api/v1/auth/refresh with stale (previously rotated) refreshToken returns 403
  </behavior>
  <files>
    apps/api/src/auth/auth.module.ts,
    apps/api/src/auth/auth.service.ts,
    apps/api/src/auth/auth.controller.ts,
    apps/api/src/auth/dto/login.dto.ts,
    apps/api/src/auth/strategies/access-token.strategy.ts,
    apps/api/src/auth/strategies/refresh-token.strategy.ts,
    apps/api/src/auth/guards/access-token.guard.ts,
    apps/api/src/auth/guards/refresh-token.guard.ts,
    apps/api/test/auth.e2e-spec.ts,
    apps/api/test/jest-e2e.json
  </files>
  <action>
    Implement AuthModule following the behavior contract above. Implementation matches RESEARCH.md Pattern 2 (two Passport strategies) and PATTERNS.md auth/* sections.

    1. Strategies & guards — copy from PATTERNS.md.
       - `access-token.strategy.ts`: `PassportStrategy(Strategy, 'jwt')`, reads `JWT_ACCESS_SECRET` via ConfigService, `validate(payload)` returns payload, export `JwtPayload = { sub: string; email: string }`.
       - `refresh-token.strategy.ts`: `PassportStrategy(Strategy, 'jwt-refresh')`, `passReqToCallback: true`, `validate(req, payload)` returns `{ ...payload, refreshToken: <raw bearer> }`.
       - `access-token.guard.ts`: `AuthGuard('jwt')`, injects `Reflector`, checks `IS_PUBLIC_KEY` first (Pitfall 3).
       - `refresh-token.guard.ts`: `AuthGuard('jwt-refresh')`.

    2. `login.dto.ts` — `@IsEmail() email; @IsString() @MinLength(8) password`.

    3. `auth.service.ts` per PATTERNS.md:
       - `validateUser(email, password)` — same `ForbiddenException('Invalid credentials')` for both missing user and bad password (no email enumeration, V2 ASVS).
       - `getTokens(userId, email)` — access (15m, JWT_ACCESS_SECRET) + refresh (7d, JWT_REFRESH_SECRET).
       - `updateRefreshToken(userId, refreshToken)` — bcrypt hash + persist.
       - `login`, `refreshTokens` (with rotation), `logout` (null out refresh_token).

    4. `auth.controller.ts` per PATTERNS.md:
       - `@Public() @Post('login')` — sets `refresh_token` cookie `{ httpOnly: true, sameSite: 'strict', secure: process.env.NODE_ENV === 'production', maxAge: 7 * 24 * 60 * 60 * 1000 }`; returns `{ accessToken }`.
       - `@UseGuards(RefreshTokenGuard) @Post('refresh')` — new accessToken + rotated cookie.
       - `@Post('logout')` — clears cookie, 204.

    5. `auth.module.ts` — imports PassportModule, JwtModule.register({}), UsersModule, ConfigModule. Providers include strategies + guards + service. Exports AccessTokenGuard.

    6. `test/jest-e2e.json` — standard NestJS E2E config; `setupFiles: ["dotenv/config"]` loading `.env.test`.

    7. `test/auth.e2e-spec.ts` — supertest covering every behavior; truncate users + insert seed user (bcrypt-hashed "Password123!") in `beforeAll`.

    Run `pnpm --filter @trainsmart/api test:e2e -- --testPathPattern=auth` — all tests must pass.
  </action>
  <verify>
    <automated>pnpm --filter @trainsmart/api test:e2e -- --testPathPattern=auth</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/auth.e2e-spec.ts` exists with `describe('Auth')` block and at least 6 `it(...)` cases mapping to the behavior list
    - `pnpm --filter @trainsmart/api test:e2e -- --testPathPattern=auth` exits 0
    - `auth.controller.ts` contains `@Public()` on login and `@UseGuards(RefreshTokenGuard)` on refresh
    - `auth.service.ts` `validateUser` throws SAME exception for missing-user vs bad-password
    - cookie config contains `httpOnly: true`, `sameSite: 'strict'`, `maxAge: 7 * 24 * 60 * 60 * 1000`
    - JWT payload contains only `sub` and `email`
  </acceptance_criteria>
  <done>
    Login, refresh (with rotation), and logout endpoints work end-to-end against the live test database. Global guard blocks unauthenticated requests except `/auth/login`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement RtoModule (CRUD + workspace provisioning + dashboard counts incl. DASH-04/05/06 placeholders) + RTO E2E tests</name>
  <read_first>
    - .planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md (§rto controller, service, dtos, §Shared Patterns)
    - .planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md (Code Examples, §Don't Hand-Roll, Pitfalls 4/5)
    - apps/api/src/auth/auth.module.ts (so executor knows the access token guard is global)
    - packages/database/prisma/schema.prisma (Rto + 8 stub models)
    - .planning/REQUIREMENTS.md (RTO-01..04, DASH-01..06 acceptance language)
  </read_first>
  <behavior>
    - Test (RTO-01): POST /api/v1/rtos with body `{ name, asqa_code, operating_states, contact_name?, contact_email?, contact_phone? }` and valid Bearer token returns 201 + created RTO with generated UUID
    - Test (validation): POST /api/v1/rtos with missing required field returns 400
    - Test (mass assignment defense): POST /api/v1/rtos with extra property `{ ..., status_color: "green" }` returns 400 (forbidNonWhitelisted)
    - Test (RTO-02): After POST /api/v1/rtos, exactly SEVEN sub-entity tables receive one stub row each, keyed to the new rto_id (atomic transaction): rto_qualifications, trainers, tas_documents, validations, documents, tasks, notes. `compliance_alerts` table receives ZERO rows (real alerts only come from Phase 5 compliance engine).
    - Test (RTO-03 + DASH-02): GET /api/v1/rtos returns an array; each element contains `_count: { alerts: number, validations: number }`. The alerts count is 0 for a freshly created RTO (no ComplianceAlert is provisioned). validations count reflects only `planned_date >= now()` non-deleted records.
    - Test (DASH-04/05/06 placeholders): GET /api/v1/rtos response objects MUST include `superseded_quals_count: 0`, `tas_review_count: 0`, `trainer_alerts_count: 0` (hardcoded literal 0 in Phase 1 — Phase 5 will replace with computed values).
    - Test (RTO-04): GET /api/v1/rtos/:id returns the RTO with stub relation arrays present (qualifications, trainers, tas_documents, validations, documents, tasks, notes each length 1; alerts length 0).
    - Test (soft delete): DELETE /api/v1/rtos/:id sets `deleted_at`; the RTO no longer appears in GET /api/v1/rtos; re-POST with the same `asqa_code` succeeds (partial unique index from Plan 01-01 Task 3).
    - Test (auth): All RTO endpoints return 401 without Bearer token.
  </behavior>
  <files>
    apps/api/src/rto/rto.module.ts,
    apps/api/src/rto/rto.service.ts,
    apps/api/src/rto/rto.controller.ts,
    apps/api/src/rto/dto/create-rto.dto.ts,
    apps/api/src/rto/dto/update-rto.dto.ts,
    apps/api/src/rto/dto/rto-response.dto.ts,
    apps/api/test/rto.e2e-spec.ts
  </files>
  <action>
    Implement RtoModule following the behavior contract above.

    1. `create-rto.dto.ts` — copy from PATTERNS.md §create-rto.dto.ts. Includes `@Matches(/^\d{5}$/)` on `asqa_code` (UI-SPEC §Error States), `@IsArray() @IsString({each:true}) @ArrayMinSize(1) operating_states`, optional contact fields with `@IsEmail` on contact_email.

    2. `update-rto.dto.ts` — `export class UpdateRtoDto extends PartialType(CreateRtoDto) {}` from `@nestjs/swagger`.

    3. `rto-response.dto.ts` — declares the DTO shape returned by findAll/findOne (per `<interfaces>`). Includes the THREE static placeholder count fields (`superseded_quals_count`, `tas_review_count`, `trainer_alerts_count`). These are typed as `number` and constructed as literal `0` in the service layer until Phase 5.

    4. `rto.service.ts`:
       - `findAll()` — `where: { deleted_at: null }`, include `_count: { select: { alerts: { where: { resolved: false, deleted_at: null } }, validations: { where: { planned_date: { gte: new Date() }, deleted_at: null } } } }`, `orderBy: { name: 'asc' }`. After Prisma query, MAP each row to add the three static placeholder fields:
         ```
         return rows.map((r) => ({
           ...r,
           superseded_quals_count: 0,   // DASH-04 placeholder — Phase 5 replaces with real computation
           tas_review_count: 0,         // DASH-05 placeholder — Phase 5
           trainer_alerts_count: 0,     // DASH-06 placeholder — Phase 5
         }));
         ```
         Each field MUST carry the inline comment above so future maintainers know it's intentional.
       - `findOne(id)` — `findFirst({ where: { id, deleted_at: null }, include: { qualifications: true, trainers: true, tas_documents: true, validations: true, documents: true, tasks: true, alerts: true, notes: true } })`. Also augment with the three placeholder count fields (same literal 0 values).
       - `create(dto)` — `prisma.$transaction(async (tx) => { ... })` that:
         a) creates the Rto row,
         b) for RTO-02 provisioning, inserts one stub row in SEVEN sub-entity tables keyed to the new rto_id: `rto_qualifications`, `trainers` (full_name "(placeholder)"), `tas_documents`, `validations` (status default "Scheduled", planned_date null), `documents` (file_key "placeholder", file_name "placeholder.txt"), `tasks` (title "(placeholder)"), `notes` (content "(workspace initialised)"). DO NOT insert into `compliance_alerts` — real alerts only come from the Phase 5 compliance engine; provisioning a stub alert would cause every new RTO to display "1 alerts" as phantom data on the dashboard.
         c) returns the created Rto.
         Add code comment marking each stub insert as `// RTO-02 workspace provisioning stub`.
       - `update(id, dto)` — `prisma.rto.update({ where: { id }, data: dto })`.
       - `remove(id)` — soft delete: `prisma.rto.update({ where: { id }, data: { deleted_at: new Date() } })`.

    5. `rto.controller.ts` — copy from PATTERNS.md §rto.controller.ts. `@Controller('rtos')`. Standard 5 REST methods. Do NOT add `@Public()` — global guard protects all.

    6. `rto.module.ts` — `controllers: [RtoController]`, `providers: [RtoService]`.

    7. `test/rto.e2e-spec.ts` — full E2E suite covering every behavior. Setup: login as seed user, capture accessToken, send Bearer in Authorization header. Use Prisma in beforeEach to truncate tables. After POST /rtos, assert:
       - 7 stub tables each have exactly 1 row keyed to the new rto_id
       - compliance_alerts table has 0 rows for the new rto_id
       - GET /rtos response includes the three placeholder count fields all equal to 0
       - GET /rtos response `_count.alerts === 0` for the freshly created RTO

    Run `pnpm --filter @trainsmart/api test:e2e` — both spec files must pass.
  </action>
  <verify>
    <automated>pnpm --filter @trainsmart/api test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @trainsmart/api test:e2e` exits 0 with both `auth.e2e-spec.ts` and `rto.e2e-spec.ts` passing
    - `apps/api/src/rto/rto.service.ts` `create()` is wrapped in `prisma.$transaction(` and inserts into exactly 7 stub tables (verifiable by grep: 7 occurrences of `tx.{model}.create(` covering rtoQualification, trainer, tasDocument, validation, document, task, note). NO `tx.complianceAlert.create(` call exists in the service.
    - `findAll()` `include._count` contains BOTH `alerts: { where: { resolved: false, deleted_at: null } }` AND `validations: { where: { planned_date: { gte: ... }, deleted_at: null } }`
    - `findAll()` returns objects that include literal `superseded_quals_count: 0`, `tas_review_count: 0`, `trainer_alerts_count: 0` (verifiable via grep on rto.service.ts)
    - `remove()` sets `deleted_at: new Date()` and never calls `prisma.rto.delete(`
    - `apps/api/src/rto/dto/create-rto.dto.ts` uses `@Matches(/^\d{5}$/)` on `asqa_code`
    - Mass-assignment test (`status_color` in body) yields 400 — proves `forbidNonWhitelisted` is active
    - E2E test asserts `_count.alerts === 0` on a freshly created RTO (no phantom alert)
  </acceptance_criteria>
  <done>
    Full RTO CRUD operational behind JWT guard, workspace auto-provisioning atomic (7 stub tables, no ComplianceAlert), dashboard counts accurate (no phantom alerts), DASH-04/05/06 placeholder count fields present, all E2E tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ NestJS API | All requests must pass through global AccessTokenGuard; refresh cookie is httpOnly + sameSite=strict. |
| NestJS API ↔ PostgreSQL | Prisma parameterises all queries; no raw SQL in this plan. |
| Public internet ↔ /auth/login | Sole `@Public()` endpoint; rate-limited in future phase (Phase 5 alerts engine). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | JWT forging | mitigate | HS256 with min-32-char secret per env var; secrets loaded via ConfigService and never logged |
| T-02-02 | Tampering | Mass assignment via `status_color` or `id` in POST/PATCH body | mitigate | `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` in main.ts strips/rejects unknown fields; explicit E2E test verifies 400 on extra fields |
| T-02-03 | Information Disclosure | Email enumeration via login error | mitigate | `validateUser` throws identical `ForbiddenException('Invalid credentials')` for both missing user and bad password |
| T-02-04 | Information Disclosure | Tokens in JWT payload | mitigate | Payload restricted to `{ sub, email }`; audited in Task 2 acceptance |
| T-02-05 | Tampering | CSRF on refresh endpoint | mitigate | `sameSite: 'strict'` on refresh cookie; CORS restricted to FRONTEND_URL |
| T-02-06 | Elevation of Privilege | Global guard bypass on every controller | mitigate | Default-deny: `useGlobalGuards` applied app-wide; only `@Public()` escapes |
| T-02-07 | Repudiation | Refresh token reuse after rotation | mitigate | `refreshTokens` compares bcrypt-hashed stored token; rotation updates hash; reused tokens fail (E2E test) |
| T-02-08 | Information Disclosure | Password hash leaked in API responses | mitigate | RtoService never includes User in responses; UsersService.findById returns only safe fields |
| T-02-09 | Denial of Service | Brute-force login | accept | <10 internal users; rate limiter deferred to Phase 5 |
| T-02-10 | Tampering | Phantom compliance alert data on dashboard | mitigate | RTO create transaction does NOT provision ComplianceAlert rows; real alerts only come from Phase 5 compliance engine. E2E test asserts `_count.alerts === 0` on a freshly created RTO. |
| T-02-SC | Tampering | npm/pnpm package installs (nestjs, passport, bcryptjs, etc.) | mitigate | RESEARCH.md §Package Legitimacy Audit verified all packages [VERIFIED: npm registry] |
</threat_model>

<verification>
- `pnpm --filter @trainsmart/api exec tsc --noEmit` exits 0
- `pnpm --filter @trainsmart/api test:e2e` exits 0
- Manual smoke (PowerShell): `Invoke-RestMethod -Method Post -Uri http://localhost:3001/api/v1/auth/login -ContentType 'application/json' -Body '{"email":"seed@example.com","password":"Password123!"}'` returns 200 + accessToken + Set-Cookie refresh_token
- `Invoke-WebRequest -Uri http://localhost:3001/api/v1/rtos` returns 401 without bearer
- Authenticated GET returns 200 with an array; each item includes `superseded_quals_count`, `tas_review_count`, `trainer_alerts_count` (all 0) and `_count.alerts === 0` on a freshly provisioned RTO
</verification>

<success_criteria>
- All Phase 1 auth (D-05) and RTO requirements (RTO-01..04) verifiable via E2E tests
- Dashboard count aggregation (DASH-02 alerts, DASH-03 validations) returned in `_count` field on every Rto in `findAll`
- DASH-04, DASH-05, DASH-06 satisfied via static placeholder count fields (`superseded_quals_count: 0`, `tas_review_count: 0`, `trainer_alerts_count: 0`) on every Rto in `findAll`
- Workspace provisioning (RTO-02) inserts stub rows atomically in a single transaction, into 7 tables (NOT compliance_alerts)
- New RTOs show 0 alerts on the dashboard (no phantom data)
- Mass assignment, email enumeration, and unauthenticated access tests all green
</success_criteria>

<output>
Create `.planning/phases/01-foundation-rto-dashboard/01-02-SUMMARY.md` when done. Include: number of E2E tests passing, any deviations from the planned implementation, and a PowerShell transcript proving login + authenticated RTO list with placeholder count fields present and alerts=0.
</output>
</content>
</invoke>