---
phase: 01-foundation-rto-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - apps/web/app/(dashboard)/rto/[id]/page.tsx
  - apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx
  - apps/web/components/rto/rto-workspace-header.tsx
  - apps/web/middleware.ts
  - apps/api/prisma/seed.ts
  - apps/api/package.json
  - .planning/phases/01-foundation-rto-dashboard/01-04-SUMMARY.md
autonomous: false
requirements: [RTO-04, DASH-01, DASH-04, DASH-05, DASH-06]
tags: [workspace, tabs, e2e, human-verify]

must_haves:
  truths:
    - "Visiting /rto/[id] redirects to /rto/[id]/qualifications"
    - "All 8 tabs render at /rto/[id]/[tab] with stub copy '[Tab] — Coming Soon'"
    - "Invalid tab segment returns 404"
    - "RTO workspace header shows back link to /, RTO name, and ASQA code badge"
    - "Visiting / unauthenticated redirects to /login (Next.js middleware)"
    - "End-to-end smoke: login → dashboard → click RTO card → workspace tabs visible → click each tab → stub copy renders"
    - "Staff can log in with the test operator account and reach the dashboard without error (seed account exists in the database; non-production credentials documented in apps/api/prisma/seed.ts, not in this plan artifact)"
  artifacts:
    - path: "apps/web/app/(dashboard)/rto/[id]/page.tsx"
      provides: "Server-side redirect to /rto/[id]/qualifications"
      contains: "redirect"
    - path: "apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx"
      provides: "Tab router rendering all 8 stubs"
      contains: "TABS"
    - path: "apps/web/middleware.ts"
      provides: "Auth gate — unauthenticated requests to protected paths redirect to /login"
      contains: "NextResponse.redirect"
    - path: "apps/api/prisma/seed.ts"
      provides: "Idempotent seed inserting Operator user with bcrypt-hashed password"
      contains: "bcrypt.hash"
  key_links:
    - from: "apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx"
      to: "8-tab list per D-10"
      via: "const TABS = [...]"
      pattern: "qualifications.*trainers.*tas.*validations.*documents.*tasks.*alerts.*notes"
    - from: "apps/web/middleware.ts"
      to: "/login redirect"
      via: "NextResponse.redirect when refresh_token cookie absent"
      pattern: "refresh_token"
---

<objective>
Wire the frontend (Plan 03) to the API (Plan 02): create the RTO workspace tab routes per D-10, add a Next.js middleware auth gate, seed an Operator user for human testing, and run the end-to-end human verification covering all 3 success criteria from ROADMAP §Phase 1.

Purpose: Plans 02 and 03 are independent islands. Plan 04 is the integration plan — it adds the only files that span both apps (middleware on web, seed on api) and validates the full user journey. The human-verify checkpoint is the gate that proves Phase 1 is shippable.

Output: Fully navigable RTO workspace with 8 tabs, auth-protected routes, seeded test user, and a green human-verify sign-off. Phase 1 deliverables match ROADMAP success criteria 1–3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-rto-dashboard/01-CONTEXT.md
@.planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md
@.planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md
@.planning/phases/01-foundation-rto-dashboard/01-UI-SPEC.md
@apps/web/app/(dashboard)/layout.tsx
@apps/web/components/layout/app-sidebar.tsx
@apps/web/lib/api.ts
@apps/api/src/auth/auth.service.ts
@packages/database/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API seed script for Operator test user</name>
  <read_first>
    - packages/database/prisma/schema.prisma (User model)
    - apps/api/src/auth/auth.service.ts (so seed uses the same bcrypt round count)
    - apps/api/package.json (so seed script registration is appended cleanly)
  </read_first>
  <files>
    apps/api/prisma/seed.ts,
    apps/api/package.json
  </files>
  <action>
    Create an idempotent seed script that inserts an Operator user used by the human-verify checkpoint and by E2E tests.

    1. `apps/api/prisma/seed.ts`:
       - Import `prisma` from `@repo/db` and `bcrypt` from `bcryptjs`.
       - `const email = 'operator@trainsmart.local'; const password = 'Password123!'; const hash = await bcrypt.hash(password, 10);`
       - `await prisma.user.upsert({ where: { email }, update: {}, create: { email, password_hash: hash, full_name: 'Operator' } });`
       - Log "Seeded user: operator@trainsmart.local / Password123!" to stdout.

    2. `apps/api/package.json`:
       - Add `scripts.seed: "ts-node prisma/seed.ts"`.
       - Add a `prisma` block at the top level: `"prisma": { "seed": "ts-node prisma/seed.ts" }` so `prisma db seed` works too.

    3. Run `pnpm --filter @trainsmart/api run seed` from repo root and confirm stdout shows the seeded line. Re-run to confirm idempotency (upsert means second run is a no-op).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$out = (pnpm --filter @trainsmart/api run seed 2>&1 | Out-String); if ($out -match 'operator@trainsmart.local') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/seed.ts` exists and uses `prisma.user.upsert` keyed on email
    - Password is hashed with `bcrypt.hash(..., 10)` (same cost factor as `auth.service.ts`)
    - Running the seed script twice in succession produces no duplicate-key error (idempotent)
    - `apps/api/package.json` `scripts.seed` is wired
    - `users` table contains exactly one row with `email = 'operator@trainsmart.local'` after seed
  </acceptance_criteria>
  <done>
    Test user available for human verification and for any future E2E test that needs a known login.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Next.js middleware auth gate and RTO workspace tab routes</name>
  <read_first>
    - .planning/phases/01-foundation-rto-dashboard/01-PATTERNS.md (§(dashboard)/rto/[id]/page.tsx, §(dashboard)/rto/[id]/[tab]/page.tsx, §Shared Patterns)
    - .planning/phases/01-foundation-rto-dashboard/01-UI-SPEC.md (§5 RTO Workspace Layout, §7 Tab Stub Copy, §10 Accessibility)
    - .planning/phases/01-foundation-rto-dashboard/01-RESEARCH.md (Pattern 6)
    - apps/web/app/(dashboard)/layout.tsx (so the new routes inherit the sidebar shell)
    - apps/web/lib/api.ts (refresh cookie name)
  </read_first>
  <files>
    apps/web/app/(dashboard)/rto/[id]/page.tsx,
    apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx,
    apps/web/components/rto/rto-workspace-header.tsx,
    apps/web/middleware.ts
  </files>
  <action>
    1. `apps/web/middleware.ts`:
       - Use `NextRequest`/`NextResponse` from `next/server`.
       - Matcher config: protect everything EXCEPT `/login`, `/_next/*`, `/api/*` (the Next.js rewrite proxy), and static files.
       - Logic: read `refresh_token` cookie from the incoming request. If absent and path is not `/login`, return `NextResponse.redirect(new URL('/login', request.url))`. If present, `return NextResponse.next()`. Note: this is a presence-only gate — the actual JWT verification is enforced by the API on every fetch. The middleware exists so unauthenticated users do not land on the dashboard shell.
       - Export `export const config = { matcher: ['/((?!login|_next|api|favicon.ico).*)'] };`

    2. `apps/web/app/(dashboard)/rto/[id]/page.tsx`:
       - Copy verbatim from PATTERNS.md §(dashboard)/rto/[id]/page.tsx. Server component using `redirect()` from `next/navigation` to `/rto/${params.id}/qualifications`.

    3. `apps/web/components/rto/rto-workspace-header.tsx`:
       - `'use client'`. Props: `{ rtoId: string }`. Uses `useQuery({ queryKey: ['rto', rtoId], queryFn: () => apiFetch(\`/rtos/\${rtoId}\`) })` to fetch RTO details.
       - Layout per UI-SPEC §5 "RTO Workspace Layout": back link "← Back" → `/`, RTO name (`text-xl font-semibold`), ASQA code Badge (variant="outline").
       - Loading state: render a `<Skeleton className="h-8 w-64" />` placeholder.
       - Error state: render "Could not load RTO details." in `text-destructive`.

    4. `apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx`:
       - Copy structure from PATTERNS.md §(dashboard)/rto/[id]/[tab]/page.tsx. Add at top: `import { RtoWorkspaceHeader } from '@/components/rto/rto-workspace-header';`.
       - `const TABS = ['qualifications', 'trainers', 'tas', 'validations', 'documents', 'tasks', 'alerts', 'notes'] as const;` (D-10 order verbatim).
       - If `!TABS.includes(params.tab)` → `notFound()` from `next/navigation`.
       - Render: `<div className="px-8 py-6">`, then `<RtoWorkspaceHeader rtoId={params.id} />`, then `<Tabs value={params.tab}>` with `<TabsList>` containing 8 `<TabsTrigger value={t} asChild><Link href={\`/rto/\${params.id}/\${t}\`}>{capitalize(t)}</Link></TabsTrigger>`. `<TabsContent value={params.tab}>` renders stub copy "{Capitalize(tab)} — Coming Soon" as heading (`text-xl font-semibold`) and "This section will be available in a future update." (UI-SPEC §7 Tab Stub copy verbatim) as body (`text-muted-foreground mt-2`).
       - For tab="tas" the heading is "TAS — Coming Soon" (per UI-SPEC §7 — "TAS" stays uppercased, not "Tas").
       - Apply `min-h-[44px]` to each `TabsTrigger` (UI-SPEC §2 touch target rule).
       - Add `aria-label="RTO workspace tabs"` to `<TabsList>` (UI-SPEC §10 accessibility).

    Run `pnpm --filter @trainsmart/web build` after changes — must compile.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok = $true; foreach ($f in @('apps/web/app/(dashboard)/rto/[id]/page.tsx','apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx','apps/web/middleware.ts')) { if (-not (Test-Path $f)) { $ok = $false } }; if (-not (Select-String -Path 'apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx' -Pattern 'qualifications.*trainers.*tas.*validations.*documents.*tasks.*alerts.*notes' -Quiet)) { $ok = $false }; if (-not (Select-String -Path apps/web/middleware.ts -Pattern 'NextResponse.redirect' -Quiet)) { $ok = $false }; if (-not (Select-String -Path apps/web/middleware.ts -Pattern 'refresh_token' -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"; pnpm --filter @trainsmart/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/middleware.ts` reads `refresh_token` cookie and redirects to `/login` when missing
    - Middleware `config.matcher` excludes `/login`, `/_next`, `/api`, `/favicon.ico`
    - `apps/web/app/(dashboard)/rto/[id]/page.tsx` calls `redirect(\`/rto/\${params.id}/qualifications\`)` from `next/navigation`
    - `apps/web/app/(dashboard)/rto/[id]/[tab]/page.tsx` `TABS` constant equals `['qualifications','trainers','tas','validations','documents','tasks','alerts','notes']` (D-10 order)
    - Invalid tab segment calls `notFound()`
    - Each TabsTrigger contains a Next.js `<Link>` (not a button onClick navigation) so tabs are deep-linkable
    - Tab heading for tab="tas" renders exactly "TAS — Coming Soon" (uppercase TAS preserved per UI-SPEC §7)
    - `pnpm --filter @trainsmart/web build` exits 0
  </acceptance_criteria>
  <done>
    All RTO workspace routes exist; dashboard auth gate active; UI-SPEC tab stub copy renders verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end human verification of Phase 1 success criteria</name>
  <what-built>
    - Plan 01-01: monorepo + Prisma schema with all 10 tables + partial unique index on asqa_code
    - Plan 01-02: NestJS API with JWT auth, RTO CRUD, workspace provisioning, dashboard counts
    - Plan 01-03: Next.js frontend with login page, dark-sidebar dashboard shell, RTO card grid, Add RTO dialog
    - Plan 01-04 (this plan): RTO workspace tab routes, auth middleware, seeded Operator user
  </what-built>
  <how-to-verify>
    Bring up the stack:
    1. Terminal A — start Postgres if not already: `docker compose up -d postgres` (or confirm cloud DB reachable).
    2. Terminal B — start API: `pnpm --filter @trainsmart/api dev` (listens on http://localhost:3001).
    3. Terminal C — start web: `pnpm --filter @trainsmart/web dev` (listens on http://localhost:3000).
    4. Confirm seed user exists: `pnpm --filter @trainsmart/api run seed` (idempotent).

    Then verify the three ROADMAP §Phase 1 success criteria:

    **SC-1: Staff can log in and see a dashboard listing all RTOs with placeholder traffic-light status**
    a) Open http://localhost:3000 in a fresh incognito window.
    b) Expected: redirected to http://localhost:3000/login.
    c) Enter `operator@trainsmart.local` / `Password123!` and click "Sign in".
    d) Expected: redirected to http://localhost:3000/. Sidebar is DARK (zinc/slate), content area is LIGHT (white/zinc-50). Empty state appears ("No RTOs yet" + "Add your first RTO client to get started." + "Add RTO" button). A gray "Status Pending" badge is shown as the placeholder traffic-light status (D-09 — real colour badge deferred to Phase 5; the gray placeholder satisfies ROADMAP SC-1 "placeholder traffic-light status").

    **SC-2: Staff can create a new RTO and it appears on the dashboard with auto-provisioned workspace structure**
    a) Click "Add RTO" — dialog opens with title "Add New RTO".
    b) Fill: Name="Acme Training Pty Ltd", ASQA Code="12345", Operating States: tick NSW and VIC, leave contact fields blank.
    c) Click "Create RTO". Dialog closes, toast "RTO created" appears, card grid now shows one card.
    d) Card displays: "Acme Training Pty Ltd" (semibold), "12345" (muted), badges "NSW" "VIC", "0 alerts" (compliance alerts are NOT auto-provisioned — real alerts come from the Phase 5 compliance engine) and "No upcoming validations" (planned_date null doesn't match gte:now filter). Also confirms: "0 superseded qualifications", "0 TAS reviews due", "0 trainer alerts" placeholder rows and "Status Pending" gray badge (D-09 — real colour badge deferred to Phase 5).
    e) Verify provisioning in DB: from terminal D run `pnpm --filter @repo/db exec prisma studio` and confirm: one row in EACH of rtos, rto_qualifications, trainers, tas_documents, validations, documents, tasks, notes — all keyed to the same `rto_id`. CONFIRM `compliance_alerts` has ZERO rows for this `rto_id` (phantom alert prevention per plan 01-02 T-02-10 — alerts only originate from Phase 5 compliance engine).
    f) Try to create another RTO with the same ASQA code "12345" — expect inline form error "An RTO with this ASQA code already exists." (proves API uniqueness check).

    **SC-3: Navigating into an RTO workspace shows tabbed sections (Qualifications, Trainers, Documents, Tasks, Alerts, Notes) — empty but structurally correct**
    a) Click the Acme Training card.
    b) Expected: URL becomes /rto/<uuid>/qualifications. Workspace header shows "← Back" link, "Acme Training Pty Ltd", ASQA badge "12345".
    c) Tab bar shows all 8 tabs in order: Qualifications | Trainers | TAS | Validations | Documents | Tasks | Alerts | Notes.
    d) Click each tab. URL updates per-click. Content area shows the heading "{Tab} — Coming Soon" (note: "TAS" is uppercase, others capitalised — "Qualifications", "Trainers", "TAS", "Validations", "Documents", "Tasks", "Alerts", "Notes") and the body copy "This section will be available in a future update."
    e) Manually type an invalid tab in the URL bar, e.g. /rto/<uuid>/invalidtab — expect a 404 page (Next.js notFound).
    f) Click "← Back" → returns to dashboard with the card still present.

    **Additional cross-cutting checks:**
    g) Refresh the page after login — should NOT redirect to login (refresh_token cookie persists session). The dashboard should re-render with the same RTO.
    h) Click the Logout icon in the sidebar footer — should redirect to /login. Navigating to / now should redirect back to /login (middleware gate works).
    i) Resize the browser to mobile width (<768px) — sidebar collapses to hamburger sheet; card grid becomes 1 column.
    j) Open DevTools → Application → Local Storage and Session Storage. CONFIRM there is NO `accessToken`, NO JWT, NO password anywhere in storage. The only auth artefact should be the `refresh_token` cookie marked HttpOnly.
  </how-to-verify>
  <resume-signal>
    Type "all three success criteria pass" to advance the phase. Otherwise describe what failed (which criterion, what you saw vs expected) so a follow-up gap-closure plan can be generated via `/gsd:plan-phase --gaps 01`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser ↔ Next.js routes | Middleware enforces presence of refresh_token cookie on protected paths. |
| Workspace tab URL ↔ NotFound | Tab segment validated against a fixed allow-list; unknown segments 404. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | Bypassing the dashboard auth gate via direct URL | mitigate | `apps/web/middleware.ts` redirects to /login when refresh_token cookie absent; the API still enforces JWT independently (defence-in-depth from Plan 02 T-02-06) |
| T-04-02 | Information Disclosure | Leaked seed credentials in repo | mitigate | Seed credentials are `operator@trainsmart.local` / `Password123!` — clearly NON-PRODUCTION placeholder; documented in summary. Production deploy MUST rotate. Add a comment to seed.ts: `// DO NOT use this credential in production — placeholder for local dev only` |
| T-04-03 | Tampering | Tab segment routing to arbitrary content | mitigate | `notFound()` triggered for any segment not in the 8-item allow-list (Task 2 acceptance) |
| T-04-04 | Spoofing | Middleware-only auth gate gives false confidence | mitigate | Middleware is a UX gate; the API enforces auth on every endpoint (Plan 02 T-02-06). Documented in code comment. |
| T-04-SC | Tampering | Dependency installs | mitigate | No new external dependencies introduced in this plan; only application code added. |
</threat_model>

<verification>
- `pnpm --filter @trainsmart/web build` exits 0
- `pnpm --filter @trainsmart/api run seed` exits 0 and is idempotent on second run
- Human-verify checkpoint signed off (Task 3)
</verification>

<success_criteria>
ROADMAP §Phase 1 success criteria all pass:
1. Staff can log in and see a dashboard listing all RTOs with placeholder traffic-light status — VERIFIED in checkpoint SC-1 (status badge intentionally hidden per D-09; "placeholder" is the data field `status_color`, not a rendered badge)
2. Staff can create a new RTO and it appears on the dashboard with auto-provisioned workspace structure — VERIFIED in checkpoint SC-2
3. Navigating into an RTO workspace shows tabbed sections — VERIFIED in checkpoint SC-3
</success_criteria>

<output>
Create `.planning/phases/01-foundation-rto-dashboard/01-04-SUMMARY.md` when done. Include: human-verify transcript or screenshots of the three success criteria, any deviations from the planned UI, seed credentials confirmed rotated-out warning for production, and the list of all 9 entity tables confirmed populated for the test RTO.
</output>
